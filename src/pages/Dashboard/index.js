import React from 'react';
import './styles.scss';

const Dashboard = props => {
    return (
        <div className="container">
            <h1>
                Logged In !
            </h1>
        </div>
    );
};

export default Dashboard;